# ==========================================
# C XML BR Engine — Credenciais de acesso
# ==========================================
# Coloque este arquivo em: .streamlit/secrets.toml
#
# Para gerar o hash de uma nova senha, rode no Python:
#   import hashlib
#   print(hashlib.sha256("sua_senha".encode()).hexdigest())
#
# Níveis disponíveis: "admin" ou "usuario"
#   admin  → acesso total
#   usuario → só PDF→XML e Preenchimento de Confirmação
# ==========================================

[usuarios]
carlos   = { senha = "240be518fabd2724ddb6f04eeb1da5967448d7e831c08c8fa822809f74c720a9", nivel = "admin" }
operador = { senha = "6a3ef924cb19135103c1e5697a04a926209911a8cd45734773fac25454e691a0", nivel = "usuario" }

# Para adicionar mais usuários, copie uma linha acima e altere o nome, senha (hash) e nivel.
# Exemplo:
# joao = { senha = "hash_aqui", nivel = "usuario" }
